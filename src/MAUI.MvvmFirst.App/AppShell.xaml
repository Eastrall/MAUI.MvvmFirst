<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Name="MainShell"
    x:Class="MAUI.MvvmFirst.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MAUI.MvvmFirst.App.Pages"
    xmlns:ViewModels="clr-namespace:MAUI.MvvmFirst.App.ViewModels"
    x:DataType="{x:Type ViewModels:AppShellViewModel}"
    NavigationPage.HasNavigationBar="False"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate local:LoginPage}"
        BindingContext="{Binding LoginViewModel}"
        Route="Login" />

    <ShellContent
        Title="Welcome Tutorial"
        ContentTemplate="{DataTemplate local:WelcomePage}"
        BindingContext="{Binding WelcomeViewModel}"
        Route="WelcomeTutorial" />

    <TabBar Route="Main">
        <Tab Route="Home" Title="Home">
            <ShellContent
                ContentTemplate="{DataTemplate local:HomePage}"
                BindingContext="{Binding HomeViewModel}"
                Route="Home" />
        </Tab>
        <Tab Route="User" Title="User">
            <ShellContent 
                ContentTemplate="{DataTemplate local:UserProfilePage}"
                BindingContext="{Binding UserProfileViewModel}" />
        </Tab>
    </TabBar>

</Shell>
